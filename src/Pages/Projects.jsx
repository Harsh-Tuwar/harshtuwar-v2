import React from 'react';
import ProjectsContainer from '../Components/ProjectsContainer';
import PageContainer from '../Components/PageContainer';

const Projects = () => {
	return (
		<PageContainer Child={ProjectsContainer} />
	);
}
 
export default Projects;